# VCS + local tooling
.git
.gitignore

# ova runtime state (pids, logs, warmup clips)
.ova

# Python envs / caches
.venv
venv
__pycache__
*.py[cod]
*.egg-info
.mypy_cache
.pytest_cache
.ruff_cache

# Secrets — never bake into the image
.env
.env.*
!.env.example

# Docker files themselves
Dockerfile
docker-compose.yml
.dockerignore
